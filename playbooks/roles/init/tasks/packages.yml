- name: Installing required packages
  apt:
    name:
      - sudo
      - rsync
      - python
      - python-pip
      - python3
      - python3-pip
      - supervisor
      - haproxy
      - memcached
      - nginx
      - rsyslog
      - logrotate
    update_cache: yes
    cache_valid_time: 86400
    state: latest
