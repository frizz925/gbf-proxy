apiVersion: apps/v1
kind: Deployment
metadata:
  name: gbf-proxy-controller
  labels:
    app: gbf-proxy
    role: controller
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gbf-proxy
      role: controller
  template:
    metadata:
      labels:
        app: gbf-proxy
        role: controller
    spec:
      containers:
      - name: gbf-proxy-controller
        image: gbf-proxy:latest
        imagePullPolicy: Never
        ports:
        - name: controller-port
          containerPort: 80
        command:
        - "gbf-proxy"
        - "controller"
        - "0.0.0.0:80"
        - "--cache-address"
        - "gbf-proxy-cache:80"
        - "--web-address"
        - "gbf-proxy-web:80"
        - "--web-hostname"
        - "gbf-proxy.kogane.moe"
        readinessProbe:
          httpGet:
            path: /
            port: controller-port
            httpHeaders:
            - name: Host
              value: gbf-proxy.kogane.moe
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: controller-port
            httpHeaders:
            - name: Host
              value: gbf-proxy.kogane.moe
          initialDelaySeconds: 15
          periodSeconds: 60
---
apiVersion: v1
kind: Service
metadata:
  name: gbf-proxy-controller
  labels:
    app: gbf-proxy
    role: controller
spec:
  ports:
  - port: 80
    targetPort: controller-port
  selector:
    app: gbf-proxy
    role: controller
